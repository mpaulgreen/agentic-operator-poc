resources:
- bases/database.postgres.example.com_postgresclusters.yaml
#+kubebuilder:scaffold:crdkustomizeresource

# [WEBHOOK] CRD conversion webhook patch removed — using strategy: None
# (conversion webhook only needed when schemas differ and require hub-and-spoke conversion)
# - path: patches/webhook_in_postgresclusters.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER]
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

configurations:
- kustomizeconfig.yaml
